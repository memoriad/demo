import React from 'react';
import CircularProgress from 'material-ui/CircularProgress';

const Progress = () => (
  <div id="progress" className="row center-align hide">
    <CircularProgress />
  </div>
);

export default Progress;
